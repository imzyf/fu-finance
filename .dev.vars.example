# Local-only secrets for `pnpm dev` / seed / drizzle-kit (gitignored — never commit).
# Neon *dev* branch connection string (not production).
DATABASE_URL="postgresql://neondb_owner:.../neondb?sslmode=require&channel_binding=require"
